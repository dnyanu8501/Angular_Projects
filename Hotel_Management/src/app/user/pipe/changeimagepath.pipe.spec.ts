import { ChangeimagepathPipe } from './changeimagepath.pipe';

describe('ChangeimagepathPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeimagepathPipe();
    expect(pipe).toBeTruthy();
  });
});
